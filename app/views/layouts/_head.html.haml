%title
  = content_for(:title) || t(:default_page_title)
%meta{name: "description", content: "#{content_for(:meta_description) || t(:default_page_description)}"}
= stylesheet_link_tag "application", media: "all"
